import React from 'react';
import { ButtonProps } from './Button.props';
interface Props {
    options: ButtonProps;
}
declare function MuiButton({ options }: Props): React.JSX.Element;
export default MuiButton;
